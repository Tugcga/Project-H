import { QuadGridIndexComponent } from "./quad_grid_index";

// this component assigned only to monsters
// it allows to track the visible quad index for each monster entity
export class VisibleQuadGridIndexComponent extends QuadGridIndexComponent {}